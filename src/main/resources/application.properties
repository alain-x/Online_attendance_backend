spring.application.name=attendance

server.port=8080

spring.datasource.url=jdbc:mysql://${DB_HOST:turntable.proxy.rlwy.net}:${DB_PORT:28825}/${DB_NAME:attendance_db}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:RmthnXupuvYQOVKMUTAYwOSlmTkyMewF}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

app.jwt.secret=${JWT_SECRET:QAU21yVhYKc9wFH701SZB62ZYiVoBsjQBMJDrGyCkItpS2EhO5rePOsKX7t6QUtYbSpItocaBQK9Di45ThVqM8u}
app.jwt.expiration-minutes=${JWT_EXP_MINUTES:480}

# Bootstrap Admin User (create first admin user on startup)
# IMPORTANT: Change these values in production or set via environment variables
app.bootstrap.admin.username=${BOOTSTRAP_ADMIN_USERNAME:alainvava54@gmail.com}
app.bootstrap.admin.password=${BOOTSTRAP_ADMIN_PASSWORD:Ba221122!!}

# Odoo integration (scaffold)
odoo.enabled=${ODOO_ENABLED:false}
odoo.base-url=${ODOO_BASE_URL:}
odoo.db=${ODOO_DB:}
odoo.username=${ODOO_USERNAME:}
odoo.password=${ODOO_PASSWORD:}
